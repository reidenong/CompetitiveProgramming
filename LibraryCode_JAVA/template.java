import java.util.*;

public class App {
    public static void main(String[] args) throws Exception {
        Scanner s = new Scanner(System.in);
        
        // Receiving integer input
        int N = s.nextInt();
        
        //Checking for subsequent lines of Integer input
        while(s.hasNextInt()) {
            // Receive input
        }
    }
}
